export const KB_IN_BYTES = 1024
export const MB_IN_BYTES = 1024 * KB_IN_BYTES

export const MAX_USER_PROFILE_IMAGE_FILE_SIZE_BYTES = 5 * MB_IN_BYTES // Tamanho limite de 5mb para as imagens de perfil dos usuários comuns

export const MAX_DOCUMENT_FILE_SIZE_BYTES = 30 * MB_IN_BYTES // Tamanho limite de 30mb para os documentos genéricos enviados

export const SEARCH_CONTENT_TEXT_LIMIT = 600 // Tamanho limite de 600 caracteres para o conteúdo de pesquisa (evitar indexar muito conteúdo)
