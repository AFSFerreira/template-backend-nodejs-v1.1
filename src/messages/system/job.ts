import type { ISystemResponse } from '@custom-types/responses/system-response'

export const JOB_NAME_ALREADY_EXISTS: ISystemResponse = {
  code: 'JOB_NAME_ALREADY_EXISTS',
  message: 'O job registrado já existe',
}
